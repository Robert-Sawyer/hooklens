# Database
DATABASE_URL="postgresql://hooklens:hooklens@localhost:5433/hooklens?schema=public"

# API
API_PORT=4000

# Later stages add web, embedding and MCP settings. Keep secrets only in
# `.env`, never commit them.
